<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mezan.zafariqbalbooks">

    <application
        android:allowBackup="true"
        android:icon="@drawable/main_bg"
        android:label="@string/app_name"
        android:roundIcon="@drawable/main_bg"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Reader"></activity>
        <activity android:name=".Others_View" />
        <activity android:name=".Auto_Biography_View" />
        <activity android:name=".Math_And_Science_View" />
        <activity android:name=".StoriesView" />
        <activity android:name=".ScienceFictionView" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>